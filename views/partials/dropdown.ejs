<div class="dropdown">
  <button onclick="myFunction()" class="dropbtn">Ordenar por ▼</button>
  <div id="myDropdown" class="dropdown-content">
    <% if (pagination.filtered == 2) {%>
    <a href="/search?keywords=<%=pagination.keyword%>&page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=name">Nombre</a>
    <a href="/search?keywords=<%=pagination.keyword%>&page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=price">Precio</a>
    <a href="/search?keywords=<%=pagination.keyword%>&page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=color">Color</a>
    <% }else{ %>
    <a href="?page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=name">Nombre</a>
    <a href="?page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=price">Precio</a>
    <a href="?page=<%= pagination.page_number+1%>&size=<%= pagination.page_size %>&order=color">Color</a>
    <% }%>

  </div>
</div>

<script>
  function myFunction() {
    document.getElementById("myDropdown").classList.toggle("show");
  }

  function filterFunction() {
    var input, filter, ul, li, a, i;
    input = document.getElementById("myInput");
    filter = input.value.toUpperCase();
    div = document.getElementById("myDropdown");
    a = div.getElementsByTagName("a");
    for (i = 0; i < a.length; i++) {
      txtValue = a[i].textContent || a[i].innerText;
      if (txtValue.toUpperCase().indexOf(filter) > -1) {
        a[i].style.display = "";
      } else {
        a[i].style.display = "none";
      }
    }
  }
</script>